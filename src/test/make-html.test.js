import { describe, expect, it } from '@jest/globals';
import { statsTemplate, makeHTML } from '../makeHTML';

describe('make-html', () => {
    it('does stuff', () => {
        const one = 1;
        const two = 2;

        expect(two-one).toBe(1);
    });
});